;
(function ($, Drupal, drupalSettings) {
    Drupal.behaviors.customwine = {
        attach(context) {
            //console.log(drupalSettings);
        }
    };
})(jQuery, Drupal, drupalSettings);
